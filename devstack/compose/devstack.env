# Copie este arquivo para .env e preencha os valores antes de executar make

# ─── Banco de Dados ────────────────────────────────────────────────────────────

POSTGRES_USER=postgres
POSTGRES_PASSWORD=Admin123
POSTGRES_PORT_HOST=5432
POSTGRES_PORT_CONTAINER=5432
POSTGRES_DB=praxis_db_dev
PGADMIN_DEFAULT_EMAIL=suasnapraxis@praxis.local
PGADMIN_DEFAULT_PASSWORD=Admin123
PGADMIN_PORT_HOST=5050
PGADMIN_PORT_CONTAINER=5050

# Hosts Proxies e Hosts de redirecionamento

NGINX_HTTP_PORT_HOST=80
NGINX_HTTP_PORT_CONTAINER=80
NGINX_HTTPS_PORT_HOST=443
NGINX_HTTPS_PORT_CONTAINER=443
NGINX_WEBADMIN_PORT_HOST=81
NGINX_WEBADMIN_PORT_CONTAINER=81
# NGINX_PROXIES_PATH=/data
# NGINX_CERTS_PATH=/etc/letsencrypt
# NGINX_PROXIES_DATA=./data
# NGINX_CERTS_DATA=./letsencrypt

# Orquestração de containers

PORTAINER_HTTP_PORT_HOST=9443
PORTAINER_HTTP_PORT_CONTAINER=9443
